import ComingSoon from "../ComingSoon";

export const metadata = { title: "Pipeline" };

export default function Page() {
  return <ComingSoon title="Pipeline" note="The kanban pipeline ships with the sales workflow release." />;
}
