import ComingSoon from "../ComingSoon";

export const metadata = { title: "Leads" };

export default function Page() {
  return <ComingSoon title="Leads" note="Lead management arrives with the next release (imports, table, dedupe)." />;
}
