// Central place where every LeadOS domain registers its job handlers.
// Imported for side effects by the cron route (and by tests).
// Domains added in later phases append their registrations here.

export {}; // no handlers yet — Phase 0 ships the queue itself
